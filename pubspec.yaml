name: vesperio
description: "Vesperio — relaxing soundscapes, sleep timers, and focus tools for better rest."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.3+24

environment:
  sdk: ^3.11.5

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # UI and Design
  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0
  flutter_animate: ^4.2.0
  shimmer: ^3.0.0

  # State Management
  provider: ^6.1.0
  
  # Audio Playback
  audioplayers: ^5.2.1
  just_audio: ^0.9.36
  audio_session: ^0.1.16

  # Firebase
  firebase_core: ^3.0.0
  firebase_analytics: ^11.0.0
  firebase_crashlytics: ^4.0.0

  # AdMob
  google_mobile_ads: ^5.0.0
  app_tracking_transparency: ^2.0.7

  # Local Storage
  shared_preferences: ^2.2.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # In-App Purchases (subscriptions via RevenueCat)
  purchases_flutter: ^8.0.0

  # Social Sharing
  share_plus: ^7.2.1
  
  # Notifications & Alarms
  flutter_local_notifications: ^21.0.0
  
  # Navigation
  get: ^4.6.6
  
  # Internationalization
  intl: ^0.19.0
  
  # Utilities
  in_app_review: ^2.0.0
  crypto: ^3.0.3
  connectivity_plus: ^5.0.1
  package_info_plus: ^9.0.1
  url_launcher: ^6.2.4
  uuid: ^4.5.3
  path_provider: ^2.0.15
  
  # Platform Specific
  permission_handler: ^11.3.1

  # Scheduled notifications (exact alarms / smart alarm)
  timezone: ^0.11.0

  # Home screen widgets
  home_widget: ^0.7.0

dependency_overrides:
  just_audio_web: ^0.4.16

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

  # Code Generation
  build_runner: ^2.4.6
  hive_generator: ^2.0.1
  mocktail: ^1.0.5

  # Icon & Splash Generation
  flutter_launcher_icons: ^0.14.3
  flutter_native_splash: ^2.4.3

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "resources/App Icon.png"
  min_sdk_android: 21
  adaptive_icon_background: "#100B26"
  adaptive_icon_foreground: "resources/App Icon.png"

flutter_native_splash:
  color: "#100B26"
  image: "resources/App Icon.png"
  image_dark: "resources/App Icon.png"
  color_dark: "#100B26"
  ios: true
  android: false
  web: false
  fullscreen: false

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/sounds/
    - assets/sounds/free/
    - assets/sounds/premium/

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list of font files to include with your application, using relative
  # paths to the font files relative to the "fonts" subdirectory and using the
  # 'family' from the font_families set above. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies, see
  # https://flutter.dev/custom-fonts/#from-packages

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
